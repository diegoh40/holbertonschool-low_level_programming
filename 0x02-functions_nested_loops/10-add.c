#include "main.h"

/**
 * add - function that adds two integers and returns the result.
 * @h: int to be added to b
 * @j: int to be added to a
 * Return: sum of a and b
 */
int add(int h, int j)
{
	int m;

	m = h + j;

	return (m);
}
