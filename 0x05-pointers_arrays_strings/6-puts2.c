#include <stdio.h>
#include <main.h>


/**
 * puts - function that prints every other character of a string
 * @s - string
 * Retur: 0
 */
void puts2(char *str) {
    *str = "0123456789";
    char s[40] = "";

    for(int i = 0; i < strlen(p); i+=2){
        _putchar(p[i]);
        s[i/2]=p[i];
    }

    
    return 0;
}
