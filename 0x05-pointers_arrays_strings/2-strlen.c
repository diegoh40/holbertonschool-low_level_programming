#include <stdio.h>
#include "main.h"

/**
 * _strlen- swaps the values of two integers
 *@a: firt integer
 *@b: Second integer
 * Return: 0
 */
int _strlen(char *s)
{
chr *s;
int len;

len = _strlen(str);

}
